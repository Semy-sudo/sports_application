import React, { useEffect, useState } from 'react';
import RegisterFormBlock2 from './RegisterFormBlock2';
import { withRouter } from 'react-router-dom';

const RegisterForm2 = ({ history }) => {

    return(
        <RegisterFormBlock2
        />
    );
};

export default withRouter(RegisterForm2);